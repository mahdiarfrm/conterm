.build/
.swiftpm/
Package.resolved
build/
dist/
release/
Conterm.app/
*.app/
*.dSYM
*.zip
GhosttyKit.xcframework/
GhosttyKit.xcframework.tar.gz
Resources/ghostty/
ICONS_FINAL.prev/
.DS_Store
.claude/
promo/
conterm_video/

# Local diagnostics and working notes (not for the public repo)
/scripts/conterm-monitor.sh
/scripts/diag-wakeups.sh
# docs/ holds two unrelated things: the published site (index.html,
# assets/) and working notes. Ignore every note by shape rather than by
# name — a new one is out of the repo from its first save, which is the
# only point at which keeping it out still works.
/docs/*.md
/Tests/ContermTests/
conterm-monitor-*.log
archive/
.build-ghostty/
